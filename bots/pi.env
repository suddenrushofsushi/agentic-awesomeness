# @pi — pi coding agent via OpenRouter. Needs `openrouter` in ~/.pi/agent/auth.json and defaultProvider/defaultModel in settings.json.
BOT_CMD=/opt/homebrew/bin/pi-acp
BOT_CWD="$ROOT"
BOT_ALLOW="ea claude codex hermes"
